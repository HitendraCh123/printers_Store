# Frontend environment variables (Vite requires the VITE_ prefix)
# This is the PUBLIC PayPal Client ID — safe to expose in the browser.
# Get it from https://developer.paypal.com/dashboard/applications/sandbox
VITE_PAYPAL_CLIENT_ID=your_paypal_client_id_here
